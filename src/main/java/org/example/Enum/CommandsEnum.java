package org.example.Enum;

/**
 * the enum CommandsEnum represents the different commands available in the memory management system.
 */
public enum CommandsEnum {
    INIT, ALLOC, FREEID, SHOW, STATS,

    RESET, HELP,
    DO_NOTHING, EXIT,
    UNKNOWN
}
